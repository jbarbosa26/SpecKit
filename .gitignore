# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp

# Accidental local Node artifacts (the teaching repo has no npm dependencies)
node_modules/
npm-debug.log*
yarn-error.log*

# Python / uv (the Specify CLI is Python-based)
__pycache__/
*.pyc
.venv/

# Local secrets; ignore rules do not prevent agent access or protect tracked files
.env
.env.*
!.env.example
*.pfx
*.p12

# Accidental scaffolding in THIS documentation repo only. In a student's separate
# application repo, track shared .specify/ artifacts and .github/skills/ instead.
booknook/
.specify/
